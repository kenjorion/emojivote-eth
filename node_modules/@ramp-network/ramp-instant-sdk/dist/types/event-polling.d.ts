import { IPurchase } from './types';
export declare function doFetchPurchase(apiUrl: string, purchaseId: number, token: string): Promise<IPurchase | null>;
export declare function delay(ms: number): Promise<void>;
