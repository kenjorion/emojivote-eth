import { InitializationOptions, Transaction, Account, Event, Unsubscribe } from './interfaces';
declare class Blocknative {
    private _storageKey;
    private _connectionId;
    private _dappId;
    private _networkId;
    private _transactionHandlers;
    private _socket;
    private _connected;
    private _sendMessage;
    private _watchedTransactions;
    private _watchedAccounts;
    transaction: Transaction;
    account: Account;
    event: Event;
    unsubscribe: Unsubscribe;
    constructor(options: InitializationOptions);
}
export default Blocknative;
